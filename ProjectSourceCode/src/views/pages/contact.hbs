{{> message}}

<!-- 🎥 Fullscreen Video Background -->
<video autoplay muted loop playsinline class="bg-video">
  <source src="/videos/background.mp4" type="video/mp4" />
  Your browser does not support the video tag.
</video>

<!-- 📬 Header Section -->
<section class="container my-5 position-relative z-3">
  <h1 class="text-center fw-bold text-glow-green mb-3">Get in Touch</h1>
  <p class="text-center subtitle-highlight mb-5">Have a question or idea? We'd love to hear from you.</p>
  
  <!-- 📨 Contact Form Section -->
  <div class="contact-container">
    <div class="contact-card">
      <div class="contact-card-header">
        <i class="fas fa-paper-plane me-2"></i>
        <h4>Send Us a Message</h4>
      </div>
      <div class="contact-card-body">
        <form method="POST" action="/contact/submit">
          <div class="mb-4">
            <label for="name" class="form-label">
              <i class="fas fa-user me-2 text-glow-icon"></i>Your Name
            </label>
            <input type="text" class="form-control" id="name" name="name" placeholder="Jane Doe" required />
          </div>
          
          <div class="mb-4">
            <label for="email" class="form-label">
              <i class="fas fa-envelope me-2 text-glow-icon"></i>Email Address
            </label>
            <input type="email" class="form-control" id="email" name="email" placeholder="you@example.com" required />
          </div>
          
          <div class="mb-4">
            <label for="message" class="form-label">
              <i class="fas fa-comment-alt me-2 text-glow-icon"></i>Message
            </label>
            <textarea class="form-control" id="message" name="message" rows="5" placeholder="Let us know how we can help..." required></textarea>
          </div>
          
          <button type="submit" class="btn btn-add-trip w-100">
            <i class="fas fa-paper-plane me-2"></i>Send Message
          </button>
        </form>
      </div>
    </div>
    
    <!-- Contact Information -->
    <div class="contact-info">      
      <div class="info-card">
        <div class="info-icon">
          <i class="fas fa-envelope"></i>
        </div>
        <div class="info-text">
          <h5>Email Us</h5>
          <p>mapmosaic3308@gmail.com</p>
        </div>
      </div>
      
      <div class="info-card">
        <div class="info-icon">
          <i class="fas fa-clock"></i>
        </div>
        <div class="info-text">
          <h5>Support Hours</h5>
          <p>Monday - Friday<br>9:00 AM - 5:00 PM EST</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  body {
    background: #0f0f0f;
    color: #eaeaea;
  }

  .bg-video {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    object-fit: cover;
    z-index: -1;
    filter: blur(10px) brightness(0.4);
    opacity: 0.85;
    pointer-events: none;
  }

  .text-glow-green {
    color: #eaffea;
    text-shadow: 0 0 15px rgba(56, 239, 125, 0.6);
  }

  .text-glow-icon {
    color: #38ef7d;
    text-shadow: 0 0 10px rgba(56, 239, 125, 0.5);
  }

  .subtitle-highlight {
    color: #e1e1e1;
    font-size: 1.1rem;
    text-shadow: 0 0 4px rgba(255, 255, 255, 0.1);
  }

  /* Contact Container */
  .contact-container {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    max-width: 900px;
    margin: 0 auto;
    padding-bottom: 3rem;
  }

  @media (min-width: 992px) {
    .contact-container {
      flex-direction: row;
    }
    
    .contact-card {
      flex: 2;
    }
    
    .contact-info {
      flex: 1;
    }
  }

  /* Contact Card */
  .contact-card {
    background: rgba(31, 31, 31, 0.85);
    border-radius: 1.5rem;
    overflow: hidden;
    backdrop-filter: blur(5px);
    border: 1px solid rgba(56, 239, 125, 0.1);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .contact-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.3);
    border-color: rgba(56, 239, 125, 0.2);
  }

  .contact-card-header {
    background: linear-gradient(135deg, rgba(56, 239, 125, 0.2), rgba(17, 153, 142, 0.2));
    padding: 1.25rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    display: flex;
    align-items: center;
  }

  .contact-card-header i {
    font-size: 1.2rem;
    color: #38ef7d;
    margin-right: 0.5rem;
  }

  .contact-card-header h4 {
    margin: 0;
    font-weight: 600;
    font-size: 1.2rem;
    color: #eaffea;
  }

  .contact-card-body {
    padding: 1.75rem;
  }

  /* Form Elements */
  .form-label {
    display: flex;
    align-items: center;
    font-weight: 600;
    color: #d8f2e0;
    margin-bottom: 0.5rem;
    font-size: 0.95rem;
  }

  .form-control {
    background-color: #2a2a2a;
    border: 1px solid #444;
    color: #fff;
    border-radius: 0.7rem;
    padding: 0.75rem 1rem;
    font-size: 1rem;
    transition: all 0.3s ease;
  }

  .form-control:focus {
    border-color: #38ef7d;
    box-shadow: 0 0 0 0.2rem rgba(56, 239, 125, 0.25);
    background-color: #2d2d2d;
  }

  .form-control::placeholder {
    color: #aaa;
    opacity: 1;
  }

  /* Button */
  .btn-add-trip {
    background: linear-gradient(to right, #38ef7d, #11998e);
    border: none;
    color: #000000;
    font-weight: 600;
    padding: 0.7rem 1.2rem;
    border-radius: 2rem;
    transition: all 0.3s ease-in-out;
    box-shadow: 0 2px 8px rgba(56, 239, 125, 0.15);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .btn-add-trip:hover {
    background: linear-gradient(to right, #11998e, #38ef7d);
    transform: scale(1.02);
    box-shadow: 0 4px 12px rgba(56, 239, 125, 0.3);
    color: #000000;
  }

  /* Contact Info Section */
  .contact-info {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .info-card {
    background: rgba(31, 31, 31, 0.85);
    border-radius: 1.5rem;
    padding: 1.25rem;
    display: flex;
    align-items: center;
    backdrop-filter: blur(5px);
    border: 1px solid rgba(56, 239, 125, 0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .info-card:hover {
    transform: translateX(5px);
    border-color: rgba(56, 239, 125, 0.2);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
  }

  .info-icon {
    background: linear-gradient(135deg, #38ef7d, #11998e);
    height: 40px;
    width: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-right: 1rem;
    flex-shrink: 0;
  }

  .info-icon i {
    color: #000;
    font-size: 1rem;
  }

  .info-text {
    flex: 1;
  }

  .info-text h5 {
    color: #eaffea;
    font-weight: 600;
    font-size: 1rem;
    margin-bottom: 0.25rem;
  }

  .info-text p {
    color: #d8d8d8;
    margin-bottom: 0;
    font-size: 0.9rem;
    line-height: 1.4;
  }
</style>
